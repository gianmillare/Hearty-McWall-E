d3.json('/deepstat').then(function(data) {
    // grab a div from deep_learning.html to input in a gif
    prediction_dict = {
        improvement: improve,
        prediction: heart
    }
    return Animation()
})